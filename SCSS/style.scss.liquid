/* ==========================================================================
   Most theme settings should be stored in Sassy CSS variables here, for reuse.
   ========================================================================== */
/*!theme settings */
$background-color:        {{ settings.background_color }};
$text-color:              {{ settings.text_color }};
$link-color:              {{ settings.link_color }};
$headerBackgroundColor:   {{ settings.header_background_color }};
$topNavIconColor:         {{ settings.topnav_icon_color }};
$topNavLinkColor:         {{ settings.topnav_link_color }};

/* Table of content */
/* 1.  Base styles
   2.  CSS transitions
   3.  Mixins
   4.  Header
*/

/* =====================
   1. Base styles
   ===================== */

html, body {
    background-color: $background-color;
    color: $text-color;
}

a { text-decoration: none; }

/* =====================
   2. CSS transitions
   ===================== */
@import url("partials/_transitions.scss");


/* =====================
   3. Mixins
   ===================== */
@import url("partials/_color-mixins.scss");


/* =====================
   4. Header
   ===================== */
#topnav {
    background: $headerBackgroundColor;

    .social_link {
        @extend %fadeInBackground;
        @extend %growTransition;
        @include opacity(0.7);
        color: $topNavIconColor;

        &:hover { @include opacity(1); }
    }

    .contact {
        border-color: #2A2A2A; //TODO change it depending on background color
    }

    .email_address,
    .phone_number {
        @extend %fadeInBackground;
        @include opacity(0.7);
        color: $topNavLinkColor;
    }

    .email_address { &:hover { @include opacity(1); } }
}

