@import url("partials/_normalize.scss");

/* ==========================================================================
   Most theme settings should be stored in Sassy CSS variables here, for reuse.
   ========================================================================== */
/*!theme settings */
$base-font-family:        {{ settings.base_font_family }};
$base-font-size:          {{ settings.base_font_size }};
$base-font-weight:        {{ settings.base_font_weight }};
$heading-font-family:     $base-font-family;
$heading-font-weight:     600;

$rem-fallback: false !default;
$rem-px-only: false !default;

/* Table of content */
/* 1.  Functions
   2.  Mixins
   3.  Bootstrap Grid
   4.  Base styles
   5.  Header
*/

/* =====================
   1. Functions
   ===================== */
@import url("partials/_functions.scss");


/* =====================
   2. Mixins
   ===================== */
@import url("partials/_mixins.scss");


/* =====================
   3. Bootstrap Grid
   ===================== */
@import url("partials/_grid.scss");


/* =====================
   4. Base styles
   ===================== */
main, footer { @extend .container }

/* Contain floats: nicolasgallagher.com/micro-clearfix-hack/ */
.clearfix:before, .clearfix:after { content: ''; display: table; }
.clearfix:after { clear: both; }
.clearfix { zoom: 1; }
.clear { clear: both; }
.clear-after { *zoom: 1; }
.clear-after:after { content: ''; display: table; clear: both; }

html, body {
    font-size: em($base-font-size);
    font-weight: $base-font-weight;
    line-height: 1;
    font-family: $base-font-family;
}

h1, h2, h3, h4, h5, h6 {
    font-family: $heading-font-family;
    font-weight: $heading-font-weight;
    text-rendering: optimizeLegibility; /* to enable ligatures and kerning */
}

/* =====================
   5. Header
   ===================== */
#topnav {
    @include rem(min-height, 30px);
    width: 100vw;

    .social_links {
        overflow: hidden;
        text-align: center;

        @include breakpoint(tablet) {
            float: left;
            text-align: left;
        }
    }

    .social_link {
        @include rem(margin-right, 5px);
        @include rem(line-height, 30px);
        font-size: em(16px);
    }

    .contact {
        width: 100%;
        text-align: center;

        @include breakpoint(tablet) {
            float: left;
            width: auto;
            text-align: left;
        }
    }

    .email_address,
    .phone_number {
        @include rem(margin-left, 10px);
        @include rem(line-height, 30px);
        font-size: em(11px);
        display: inline-block;

        &:before {
            @include rem(margin-right, 10px);
            content: "|";
        }

        &:first-child { margin: 0; }
        &:first-child:before {
            content: "";
            margin: 0;
            display: inline-block;
        }

        @include breakpoint(tablet) {
            float: left;

            &:first-child { @include rem(margin-left, 10px); }

            &:first-child:before {
                @include rem(margin-right, 10px);
                content: "|";
                display: inline-block;
            }
        }
    }

    .cart-link {
        @include rem(font-size, 11px);
        @include rem(line-height, 30px);
        float: right;

        .fa-shopping-cart {
            @include rem(margin-right, 5px);
            @include rem(font-size, 16px);
        }
    }
}